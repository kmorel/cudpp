# Find targets include, which should be in the same directory as this.
get_filename_component(_dir "${CMAKE_CURRENT_LIST_FILE}" PATH)
include("${_dir}/cudpp-targets.cmake")

set(cudpp_INCLUDE_DIRS "@cudpp_INCLUDE_DIRS@")
